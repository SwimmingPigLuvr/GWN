use std::io;
use std::time::Instant;
use std::cmp::Ordering;
use rand::Rng;

fn main() {
    println!("__getGoodWithNumbers__");
    println!("======================");
    println!(".  solve in ur head  .");
}
